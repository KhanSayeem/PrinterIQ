{
  "name": "printeriq-database",
  "private": true,
  "type": "module",
  "scripts": {
    "seed": "tsx seed.ts",
    "migrate": "node migrate.mjs",
    "migrate:baseline": "node migrate.mjs --baseline",
    "test": "node --test"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
